# Dependencies
node_modules/
# package-lock.json is committed on purpose — the Docker build runs `npm ci`,
# which refuses to install without it.

# Environment variables
.env
.env.local
.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build
dist/
build/

# Uploads (if storing locally)
uploads/

# Catalogue-rebuild working files (local state and data dumps, not code)
backups/
scripts/.live-fingerprints.json
scripts/.upload-progress.json
scripts/.switch-state.json
scripts/.switch-state.json.reverted
scripts/product-image-audit.json
scripts/redirect-map.json
scripts/new-catalogue-133.json
